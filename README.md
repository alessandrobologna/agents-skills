# Agent Skills

Public allowlist for selected personal agent skills.

The live skills directory may contain private or local-only skills. This publish branch starts with no skill directories tracked; add a skill by allowlisting its directory in `.gitignore` and then staging that directory explicitly.
